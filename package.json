{
  "name": "xypriss-compressible",
  "version": "2.0.19",
  "description": "Compressible Content-Type / mime checking (XyPriss Hardened)",
  "author": "Nehonix Team",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "LICENSE",
    "README.md"
  ],
  "dependencies": {
    "mime-db": ">= 1.54.0 < 2"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "test": "mocha --reporter spec --bail --check-leaks test/",
    "prepublishOnly": "xfpm run build"
  }
}
